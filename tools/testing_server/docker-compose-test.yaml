name: tp0
services:
    client-test:
        container_name: client-test
        image: testing-server:latest
        entrypoint: /test_client.sh
        networks:
            - testing_net
networks:
    testing_net:
        ipam:
            driver: default
            config:
                - subnet: 172.25.125.0/24
